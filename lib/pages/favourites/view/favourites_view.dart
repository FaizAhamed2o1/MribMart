import 'package:flutter/material.dart';

class FavouaritesScreen extends StatelessWidget {
  const FavouaritesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favourites'),
      ),
    );
  }
}
